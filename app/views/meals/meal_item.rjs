test_output = 
  @template.fields_for MealItem.new do |f|
    render :partial => 'meal_items/meal_item_fields', :object => Meal.new, :locals => {:meal_item_form => f}
  end



page.insert_html :top, "#meal-items", test_output

