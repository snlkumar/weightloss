<div class="grid_12">
  <%= link_to 'Back', admin_foods_path %>
  
  <h3>Editing food</h3>
  
  <% form_for(@food) do |f| %>
    <%= f.error_messages %>
    
    <%= render :partial => 'form', :locals => {:f => f} %>
    
    <p>
      <%= f.submit 'Update' %>
    </p>
  <% end %>
  
  <%= link_to 'Back', admin_foods_path %>
  
</div>