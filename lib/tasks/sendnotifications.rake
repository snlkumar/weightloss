task :sendnotifications0 => :environment do
								 obj = NotificationsController.new
							 	 obj.bodyfatAutoNotifications(6)
							  end
task :sendnotifications1 => :environment do
								 obj = NotificationsController.new
							 	 obj.bodyfatAutoNotifications(6)
							  end
task :sendnotifications2 => :environment do
								 obj = NotificationsController.new
							 	 obj.bodyfatAutoNotifications(6)
							  end
task :sendnotifications3 => :environment do
								 obj = NotificationsController.new
							 	 obj.bodyfatAutoNotifications(6)
							  end
task :sendnotifications4 => :environment do
								 obj = NotificationsController.new
							 	 obj.inactivityAutoNotifications(8)
							  end
task :sendnotifications5 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(9)
							  end
task :sendnotifications6 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(10)
							  end
task :sendnotifications7 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(10)
							  end
task :sendnotifications8 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(10)
							  end
task :sendnotifications9 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(10)
							  end
task :sendnotifications10 => :environment do
								 obj = NotificationsController.new
							 	 obj.inactivityAutoNotifications(11)
							  end
task :sendnotifications11 => :environment do
								 obj = NotificationsController.new
							 	 obj.inactivityAutoNotifications(11)
							  end
task :sendnotifications12 => :environment do
								 obj = NotificationsController.new
							 	 obj.inactivityAutoNotifications(11)
							  end
task :sendnotifications13 => :environment do
								 obj = NotificationsController.new
							 	 obj.inactivityAutoNotifications(11)
							  end
task :sendnotifications14 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(12)
							  end
task :sendnotifications15 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(12)
							  end
task :sendnotifications16 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(12)
							  end
task :sendnotifications17 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(12)
							  end
task :sendnotifications18 => :environment do
								 obj = NotificationsController.new
							 	 obj.inactivityAutoNotifications(13)
							  end
task :sendnotifications19 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(14)
							  end
task :sendnotifications20 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(15)
							  end
task :sendnotifications21 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(16)
							  end
task :sendnotifications22 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(17)
							  end
task :sendnotifications23 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(18)
							  end
task :sendnotifications24 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(19)
							  end
task :sendnotifications25 => :environment do
								 obj = NotificationsController.new
							 	 obj.weightAutoNotifications(20)
							  end
