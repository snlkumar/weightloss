<div class='user'>
  <div class='icon'><%=link_to avatar(user, :size => 60), "/users/#{user.friendly_id}" if user%></div>
  
  <div class='username' style="position:relative; bottom:0;font-size:12px;"><%=link_to user, "/users/#{user.friendly_id}" if user%></div>
</div>
